<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script
  </head>
  <body>
    <div class="main w-full bg-zinc-900 min-h-screen ">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none" type="text" placeholder="Title of task goes here..."  name="title"/>
          <textarea class="block  mt-2 outline-none px-5 py-2 bg-zinc-800 resize-none w-full rounded-md" placeholder="Write task details here..." name="details"></textarea>
          <input class="mt-3 bg-blue-600 px-5 py-2 rounded-md"  type="submit"  value="Create Task"/>
        </form>
      </div>
      <div class="tasks p-10 flex gap-6 flex-wrap">
        <% if(files.length>0){ %>
           <% files.forEach(function(val){%>
            <div  class="task  w-72 px-3 py-4 bg-zinc-800 rounded-md">
              <h1 class="text-white text-3xl tracking-tighter"><%=val %></h1>
              <a href="/file/<%= val %>" class="text-blue-500 inline-block mt-3">Read More</a>
            </div>
           <%}) %>
           <%} else {%>
            <h3 class="text-zinc-500">No tasks Yet..</h3>

        <%} %>
        
      </div>
    </div>
   
  </body>
</html>
